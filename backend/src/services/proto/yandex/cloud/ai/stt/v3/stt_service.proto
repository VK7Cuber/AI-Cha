syntax = "proto3";

package speechkit.stt.v3;

import "yandex/cloud/ai/stt/v3/stt.proto";

// Минимальный протокол для стриминга распознавания речи.
service Recognizer {
  rpc RecognizeStreaming (stream StreamingRequest) returns (stream StreamingResponse) {}
}
